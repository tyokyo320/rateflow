apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rateflow

resources:
  # Recommended: Use fetch-matrix for batch fetching all currency pairs
  - worker-cronjob-matrix.yaml

  # Legacy: Individual currency pair jobs (kept for reference)
  # - worker-cronjob.yaml
  # - worker-cronjob-jpy-usd.yaml

images:
  - name: tyokyo320/rateflow-api
    newTag: v1.5.2  # Updated to v1.5.2, now includes worker binary
