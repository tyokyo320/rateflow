apiVersion: v1
kind: Secret
metadata:
  name: rateflow-secret
  namespace: rateflow
type: Opaque
stringData:
  DB_PASSWORD: "your_secure_password"  # Change to your actual password
  REDIS_PASSWORD: ""  # Set if Redis requires password
