apiVersion: v1
kind: Service
metadata:
  name: rateflow-web
  namespace: rateflow
  labels:
    app: rateflow
    component: web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: rateflow
    component: web
